export default function Home() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        color: "var(--color-silver)",
      }}
    >
      <p>API de verificação de OAB — Social Jurídico.</p>
    </main>
  );
}
